# 星詠みソリティア -Shidasu- 今後の改善計画

現時点では未着手・未検討のアイデアメモ。着手する際は`superpowers:brainstorming`から設計を詰める。

1. **護符の効果の検討**
   `docs/shidasu/done/shidasu-gofu-candidates.md`の護符100個は実装完了(2026-08-03)。
   「直接点の護符」を無くして別の効果にする→対象6個(沈着・冷静・慢心・残響・流星・誠実)の変更候補12案を洗い出し(詳細は`docs/shidasu/done/shidasu-direct-score-talismans-redesign.md`を参照)、6護符すべて案Bを採用して実装完了(2026-08-06)。詳細は`docs/superpowers/specs/2026-08-06-shidasu-direct-score-talismans-plan-b-design.md`を参照。
   「報酬増加系の護符」「ワンパン軸の護符」の追加を検討する→報酬増加系の名前候補80個を洗い出し済み(2026-08-06)。詳細は`docs/shidasu/shidasu-reward-talismans-name-candidates.md`を参照。効果検討・ワンパン軸の名前検討・実装は未着手。
   合計で１５０枚になるようにする(現状98枚、残り52枚を新規検討)。
2. **報酬の計算方法の検討**
   Waveクリア時の報酬は「星による報酬」「護符による報酬」「ゲームプレイ内容による報酬」の3種類に整理。星による報酬は実装済み(`currentStar.reward`)、護符による報酬は項目1「報酬増加系の護符」で検討予定。ゲームプレイ内容による報酬(コンボ・役・全消しなどのプレイ内容軸)は将来的に項目3「レリックの追加の検討」側で扱う方針とし、効率軸(山札残り枚数・手数など)を含め本項目としては現状保留。あわせて2026-08-11、Waveクリアごとに毎回付与していた基礎報酬`waveClearAmount`(既定5)を廃止し、通貨報酬は星の`reward`のみになるよう変更済み。
3. **レリックの追加の検討**
   レリックは、ショップ販売価格の減少、ショップ商品提示数の増加、リロールコストの減少、秘儀・天啓・神託の所持上限増加などランを通して効果があり、護符の守備範囲外（ゲームプレイに直接かかわる部分以外）で有利になる効果を持つアイテム。システムの仕組み・個別候補13種の実装は完了済み(詳細は「完了済み(履歴)」を参照)。
   今後の検討課題:
   - Waveクリア報酬系レリック(数珠・千社札・算盤)は現状いずれも「難しくやるほど得」型の条件のみ。項目2で保留中の効率軸(山札残り枚数・手数など、少ない消費でクリアするほど得)を条件とした反転型のレリックを追加候補として検討する
   - 候補メモ: 天啓候補審査(項目3)で不採用となった「オファー拡張」(次回ショップのオファー数を一時的に増やす)は、天啓ではなくレリックの効果候補として転用できる(詳細は`docs/shidasu/done/shidasu-revelation-candidates.md`の候補24を参照)
   - 護符「暗雲」・天啓「虚」の削除(2026-08-13)により、レリック候補「独楽」(Wave開始時、場札に配る行数を+nする)の機能重複が解消し、レリック化検討が再開可能になった
4. **コードのリファクタリング**
   過去10回にわたるリファクタセッション(詳細は「完了済み(履歴)」を参照)で手をつけていない範囲を対象に、shidasuソース全体を改めて調査し、安全に共通化できる候補3件(shop.tsの価格関数統一・riteEffects.tsの再配布ループ共通化・revelationEffects.tsのtargetColガード共通化)を洗い出し済み(2026-08-18)。詳細は`docs/shidasu/shidasu-refactor-candidates.md`を参照。3件とも実装完了(詳細は「完了済み(履歴)」参照)。今後さらに追加候補が見つかれば同様の調査を継続する。
5. **妨害行動の裏向き演出**
   「総戻し」「一列戻し」「捨て札埋没」「護符並び替え」の裏向き挙動(データ・表示)は実装完了(2026-08-18)。続けて「総戻し」「一列戻し」の裏向き化・表向き化(革霧)に伴うアニメーション(山札への収束演出・裏向き配布演出・一番上のフリップ演出)、および「大量放出」「少量放出」の山札→捨て札裏向き移動+フリップ/据え置き演出も実装完了(2026-08-18/2026-08-19、詳細は「完了済み(履歴)」を参照)。「捨て札埋没」は場札への配布を伴わないため演出は対象外のまま。

## 完了済み(履歴)

以下は着手済み・完了済みの改善項目。詳細は各specを参照。

- **アイテムのモチーフ・名称体系**: 秘儀=ルーン文字、天啓=二十八宿、神託=八卦で統一命名済み。護符は自由命名(`docs/shidasu/done/shidasu-gofu-candidates.md`参照)。
- **アイテムの種類・効果の拡充**: 役ボーナス緩和(階段・列一掃)、同スート・同色・同ランク軸、コンボ軸、全消し軸、詰み救済軸、ワイルド供給アイテムまで実装済み(約80種の護符)。`docs/superpowers/specs/2026-07-09-shidasu-item-hard-yaku-relax-design.md`ほか多数。
- **デバッグ用専用画面**: `/admin/shidasu-debug`として実装済み。`docs/superpowers/specs/2026-07-15-shidasu-debug-sandbox-design.md`ほか。
- **ステージ構成(旧ボスWave構想)**: 旧`BossKind`階級制を廃止し、Wave単位の「星」(`stars`)による目標倍率・報酬・制限ルールの仕組みに再構築済み。`docs/superpowers/specs/2026-07-28-shidasu-run-structure-redesign-design.md`、`2026-07-30-shidasu-boss-tiers-removal-design.md`ほか。
- **ゲーム内通貨・ショップ**: 通貨(星片)を新設し、Wave終了ごとのショップで護符・秘儀・天啓・神託を購入できる仕組みを実装済み。`docs/superpowers/specs/2026-07-22-shidasu-currency-design.md`、`2026-07-23-shidasu-shop-design.md`ほか。
- **エフェクト・アニメーション**: カードプレイ移動・Wave開始配布・得点内訳表示・得点パーツ演出・片付け演出などを実装済み。`docs/superpowers/specs/2026-07-27-shidasu-play-card-animation-design.md`ほか多数。
- **護符の並べ替えUI**: ショップ画面から所持護符をドラッグ&ドロップ(PC・スマホ両対応)で並べ替えられるようにした。並べ替え結果はプレイ中の常設バッジ表示にも反映される。`docs/superpowers/specs/2026-07-31-shidasu-item-reorder-design.md`。
- **護符100個の実装完了**: `docs/shidasu/done/shidasu-gofu-candidates.md`記載の護符候補100個すべてを実装済み。誓約・契り・紅蓮・漆黒(色/スート拡張解釈を含む)は`docs/superpowers/specs/2026-08-02-shidasu-color-suit-lock-and-enablers-design.md`、白銀・果断・星霜は`docs/superpowers/specs/2026-08-03-shidasu-remaining-talismans-silver-audacity-frost-design.md`を参照。
- **得点内訳パーツの対象カードハイライト**: 得点内訳アニメーションで、同スート・同色・階段・フラッシュ・ロイヤル・同ランク・コンプリートランのパーツが中央表示されている間、対象カードを黄色枠で強調表示するようにした。`docs/superpowers/specs/2026-08-06-shidasu-score-part-card-highlight-design.md`。
- **得点内訳パーツの対象護符ハイライト**: 得点内訳アニメーションで、護符効果由来のパーツ(gained・clearBonus両チャンネル)が中央表示されている間、対応する護符バッジを黄色枠で強調表示するようにした。水鏡のエコー分は水鏡自身をハイライトする。`docs/superpowers/specs/2026-08-06-shidasu-score-part-item-highlight-design.md`。
- **役の再検討(完了)**: 難易度別(簡単・中・難しい)の新規役・パターン候補12個を洗い出し(`docs/shidasu/done/shidasu-role-candidates.md`)、うち新規役「ペア」(チェーン全体で同ランク組が2組以上成立で組数×50点加算)と新規パターン「交互」(チェーン内の実カードが4枚以上赤黒交互で80点加算、同スート・同色・階段に並ぶ第4のパターン)の2個を採用・実装した。`docs/superpowers/specs/2026-08-06-shidasu-pair-alternating-roles-design.md`。あわせて神託(oracle)のモチーフを「八卦」(8種固定)から「六十四卦」(将来の拡張余地あり)に変更し、ペア・交互を含む10役全ての卦名を再割り当て、`ORACLE_POOL`にも追加した。`docs/superpowers/specs/2026-08-06-shidasu-oracle-hexagrams-design.md`。残り10個の役・パターン候補は不採用。
- **全消しボーナスのgained計算統合**: 全消しボーナスを、コンボ倍率を通さない別枠(`lastBonusGains`)加算から、そのプレイの獲得点(gained)計算の内訳に統合した。コンボ倍率・献身・勤勉・加護・星霜・残響・慢心などの乗算、および星の得点ロックの影響も受けるようになった。`lastBonusGains`(`BonusGain`型)は不要になったため完全削除した。`docs/superpowers/specs/2026-08-06-shidasu-clear-bonus-gained-integration-design.md`。
- **トランプセット福袋の実装**: `docs/shidasu/done/shidasu-card-set-pack-candidates.md`の候補11ジャンルを再検討し、枚数バリエーションを独立ジャンルとして扱う方針のもと23種類のセットジャンル(階段・同ランク・絵札・同スート・ロイヤル・フラッシュ・コンプリートラン・ペア・赤黒バランス・ワイルド)を確定して実装した。新しい`packKind: 'cardSet'`の福袋(バラ売り無し、3-1/5-1/7-2パターン)を開けるとジャンルが重み付き抽選(重み=1/枚数、ワイルドのみ例外で1/6)で提示され、選ぶと即座に`RunState.deckComposition`へ永続追加される。`docs/superpowers/specs/2026-08-08-shidasu-card-set-pack-design.md`。
- **ショップ品ぞろえリロールの実装**: ショップ画面にバラ売り3枠+福袋2枠を一括で再抽選する「リロール」ボタンを追加した。売り切れ済みの枠も含めて全て新しい商品に入れ替わる(既存の`rollShop`を再利用)。コストは通貨消費で、同一ショップ訪問中のリロール回数(`shopRerollCount`)に応じて`shop.rerollCostStep`(既定5)ずつ増額し、次のショップに入るたびにリセットされる。`/admin/shidasu-currency`でコスト刻み幅を編集可能。`docs/superpowers/specs/2026-08-08-shidasu-shop-reroll-design.md`。
- **福袋のオブジェクト化**: ハードコードされていた福袋パターン(`PACK_DEFINITIONS`固定14種+`packPrice`価格表)を廃止し、`ShidasuParams.shop.packCatalog`という名前・種別・選択肢数・取得数・価格を持つ可変長リストに置き換えた。新規管理画面`/admin/shidasu-packs`から福袋の追加・削除・編集ができ、同じ内容のエントリを名前だけ変えて複数用意すると、その福袋の抽選確率を相対的に上げられる(各エントリの抽選確率自体は均等)。`docs/superpowers/specs/2026-08-08-shidasu-pack-catalog-design.md`。
- **天啓候補の検討・Phase A実装(カード変換・場札操作系8個)**: `docs/shidasu/done/shidasu-revelation-candidates.md`の天啓候補33個(既存秘儀からの転用9個を含む)を1つずつ採用・不採用・修正判断し、採用15個をPhase A(8個)・Phase B(7個)に分割した。Phase Aは`wave`・`deckComposition`を書き換える効果(隣列連鎖変換・4色循環変換・階段整列・全列トップ廃棄・極値ワイルド化・雷光・対話・賜物)で、`DeckCard.removed`フラグ(廃棄=デッキから永久除外、配列は縮めずフラグ管理)を新設して実装した。`docs/superpowers/specs/2026-08-09-shidasu-revelation-phase-a-design.md`。
- **天啓Phase B実装(即時報酬獲得系7個)**: 護符獲得・星片倍化・天啓回帰・神託獲得・天啓連続獲得・護符換金・秘儀回帰の7個を実装した。`wave`/`deckComposition`ではなくRunStateレベル(通貨・護符・秘儀・天啓・神託の所持リスト)を操作するため、`applyRevelationEffect`側は全てno-opにし、新規関数`grantRevelationReward`(engine.ts)で実処理を行う構成にした。天啓回帰は自己参照ループ(使用するたび自分自身を再取得し続ける)を、使用履歴フィールドの更新を自分自身の使用時だけスキップする方式で構造的に防止した。天啓は28宿中27宿(Phase A・B合計15個追加、残り1宿は未定のまま温存)まで実装完了。`docs/superpowers/specs/2026-08-09-shidasu-revelation-phase-b-design.md`。
- **秘儀の追加の検討・実装完了**: 2026-08-09、天啓候補審査(項目3)の過程で「天啓=カード変換系、秘儀=それ以外」という将来方針が決まり、既存秘儀のうちカード変換系9個を2026-08-11に秘儀の実装から一度削除した(3個は天啓側に転用済み、6個は完全削除)。同日中に、削除で空いた9枠に`docs/shidasu/done/shidasu-rite-redesign-candidates.md`セクションA(天啓候補審査で不採用となった10候補)の内容で新しい効果を設計・実装し、秘儀は全24種に復帰した。
- **レリックシステムの実装・個別候補13種の実装完了**: レリックは、ショップ販売価格の減少、ショップ商品提示数の増加、リロールコストの減少、秘儀・天啓・神託の所持上限増加などランを通して効果があり、護符の守備範囲外（ゲームプレイに直接かかわる部分以外）で有利になる効果を持つアイテム。2026-08-12、システムの仕組み(データ構造・ショップ専用枠での単品購入・所持数無制限かつ重複不可・売却不可・「付喪化」という個体ごとの進化状態・バッジ表示)を設計・実装した(詳細は`docs/superpowers/specs/2026-08-12-shidasu-relic-system-design.md`を参照)。続けて同日中に、個別レリック候補13種(招き猫・福だるま・熊手・数珠・招き布袋像・破魔矢・千羽鶴・福笹・開運こけし・縁起小槌・縁起鈴・千社札・算盤)の効果・名前を設計し、全て実装完了した(詳細は`docs/shidasu/done/shidasu-relic-candidates.md`を参照)。動作確認用の仮レリック`placeholder`は削除済み。
- **護符「暗雲」・天啓「虚」の削除**: レリック候補検討(2026-08-12)で、レリック候補「独楽」(Wave開始時、場札に配る行数を+nする)が既存の護符「暗雲」・天啓「虚」と同じ`extraTableauRows`の仕組みを使っており機能重複することが判明し、保留していた。2026-08-13、`extraTableauRows`のみを効果とする単機能アイテムであるこの2つを削除した(新規効果の割り当ては行わず単純削除)。護符は98種、天啓は28宿中26宿に更新。独楽のレリック化検討は別セッションで再開する。
- **天啓「虚」(レリック付喪化)の実装**: レリックシステム設計(2026-08-12)で保留していた「付喪化のトリガーとなる天啓」を、2026-08-13に天啓「虚」として実装した。所持レリックの中から選んだ1つ(未付喪化のもの限定)を付喪化させる選択式の効果で、`useRevelation`の新規`targetRelicId`引数と`grantRevelationReward`の`kyo`ケースで実現している。天啓は28宿中27宿に復帰。詳細は`docs/superpowers/specs/2026-08-13-shidasu-relic-tsukumoka-revelation-design.md`を参照。
- **天啓「鬼」(レリックランダム獲得)の実装**: 二十八宿のうち唯一未割当だった「鬼」を、2026-08-13に未所持のレリックをランダムに1つ獲得する効果として実装した。既存の護符ランダム獲得天啓「星」と同型のロジック(`grantRevelationReward`の`oni`ケース)。これにより二十八宿28宿全ての実装が完了した。詳細は`docs/superpowers/specs/2026-08-13-shidasu-relic-random-acquire-revelation-design.md`を参照。
- **天啓の追加の検討**: Phase A・B実装、候補審査、「虚」の削除・付喪化効果としての再実装、「鬼」の実装(いずれも本セクション内に個別記載)を経て、2026-08-13に二十八宿28宿全ての効果実装が完了し、検討事項として完了した。
- **星の妨害行動の実装**: waveSlot3の星(現行6種)に、一定ターンごとに能動的にゲーム状態を崩す「妨害行動」の仕組み(型定義・発動サイクル・封印・UI表示)を実装し、候補33個(操作対象11種×各3案)のうち各対象1個ずつ・計11個(山札・場札・チェーン・コンボ・護符・秘儀・天啓神託・レリック・捨て札・通貨・役ステータス)を先行実装した。2026-08-13、既存waveSlot3の星6種全てを対象(`sabotage: { kind: 'all' }`)にした。残り22候補・新規の星の追加・intervalTurnsの数値調整は別セッションで検討する。詳細は`docs/superpowers/specs/2026-08-13-shidasu-star-sabotage-design.md`を参照。
- **星の妨害行動 Phase A実装(残り22候補のうち11個)**: 上記で先行実装した11個に続き、2026-08-14に山札(少量放出・山札攪拌)・場札(総戻し・総入れ替え)・チェーン(チェーン部分放棄・チェーン入れ替え)・コンボ(コンボ削減・コンボ頭打ち)・護符(護符没収)・秘儀(秘儀没収・秘儀強制発動)の11個を実装し、`SABOTAGE_POOL`は計22件になった(既存waveSlot3の星6種は全て`sabotage: { kind: 'all' }`のため自動的に対象へ加わった)。候補一覧のうち「レリック封印」は、レリックの効果がゲームプレイに直接影響しないため一時封印しても体感できる変化が薄いという理由で不採用とし対象外とした。残り10個(護符並び替え・天啓/神託没収・天啓/神託強制発動・付喪化解除・捨て札消去・捨て札埋没・報酬減少・通貨強制消費・役減衰・役偏重、Phase B)は別セッションで検討する。詳細は`docs/superpowers/specs/2026-08-14-shidasu-star-sabotage-phase-a-design.md`を参照。
- **triggerSabotageの個別関数化リファクタ**: Phase Aまでで22ケース・約190行に肥大化していた`triggerSabotage`のswitch文を、`applyRiteEffect`・`applyRevelationEffect`と同じパターン(個別`applyXxx`関数+`Record<SabotageActionId, Handler>`ディスパッチ)に整理する純粋リファクタを実施した。新規ファイル`sabotageEffects.ts`に切り出し、Recordマップ化によりTypeScriptがハンドラの登録漏れをコンパイル時に検出できるようになった(switch文には無かった網羅性チェックを獲得)。挙動は一切変更していない。詳細は`docs/superpowers/specs/2026-08-14-shidasu-sabotage-effects-refactor-design.md`を参照。
- **星の妨害行動 Phase B実装(残り10候補)**: Phase Aの残り10個、護符(護符並び替え)・天啓/神託(天啓・神託没収、天啓・神託強制発動)・レリック(付喪化解除)・捨て札(捨て札消去、捨て札埋没)・資産(報酬減少、通貨強制消費)・役ステータス(役減衰、役偏重)を実装し、`SABOTAGE_POOL`は計32件になった。あわせて既存の秘儀強制発動(`riteForceActivate`)を、`applyRiteEffect`直接呼び出しから`useRite`(通常のプレイヤー操作による使用処理と同一の関数)をそのまま呼ぶ方式に改修し、「強制発動はプレイヤー操作を介さず通常使用と全く同じ処理を実行する」という方針に統一した(果断・星霜の加算、秘儀回帰の履歴も通常使用と同様に反映されるようになった)。天啓・神託強制発動も同じ方針で`useRevelation`/`useOracle`をそのまま呼ぶ(循環import回避のため`SabotageContext`へ依存性注入する設計にした)。役偏重の実装にあたり、役封印の実効レベル計算(`SealedRoleEffect`)に`multipliers`という汎用倍率フィールドを追加し、`zeroRoles`/`oracleBaselineRole`(0倍/基準値1倍)に加えて任意倍率(2倍/0.5倍など)を表現できるよう拡張した。詳細は`docs/superpowers/specs/2026-08-14-shidasu-star-sabotage-phase-b-design.md`を参照。
- **福袋pick/confirm/cancel/closeフロー共通化リファクタ**: `engine.ts`(1811行)・`+page.svelte`(1208行)に、福袋(護符item・秘儀rite・天啓revelation・神託oracle・カードセットcardSet)5カテゴリ分の開封処理がほぼ同型のまま並んでいたのを整理する純粋リファクタを実施した。`engine.ts`側は末尾の共通処理(オファー配列からの除去+残数管理+shop遷移、選択放棄時のリセット)だけを`resolvePackOfferPick`・`closePackOfferSelect`の2つのヘルパーに切り出し、容量チェック・スワップ相手の解決・use/holdモードなどカテゴリごとに本質的に異なるロジックはそのまま残した。`+page.svelte`側は副作用の無い16個の1行ハンドラを`bindRunAction`系ファクトリ経由の生成に置き換えた。公開APIのシグネチャは一切変更していない。挙動は一切変更していない。詳細は`docs/superpowers/specs/2026-08-15-shidasu-pack-flow-common-refactor-design.md`を参照。
- **売却・価格関数・buy/sellハンドラ共通化リファクタ**: 福袋リファクタの続きとして、`engine.ts`の売却系4関数(`sellItem`/`sellRite`/`sellRevelation`/`sellOracle`)を`sellFromArray`ヘルパーに、`shop.ts`の秘儀・天啓・神託の価格関数6個(rite/revelation/oracleのbuy/sell)を`categoryPrice`ヘルパーに、それぞれ委譲する純粋リファクタを実施した。`+page.svelte`のbuy系6個・sell系4個のハンドラは、福袋リファクタで導入済みの`bindParamsRunAction`ファクトリをそのまま再利用した(新規コード不要)。ショップ購入(`buyIndividualXxx`)系5関数はカテゴリごとの差異(kindチェックの有無・容量チェック関数名の違いなど)が大きいためスコープ外とした。公開APIのシグネチャ・挙動は一切変更していない。詳細は`docs/superpowers/specs/2026-08-15-shidasu-sell-price-handler-common-refactor-design.md`を参照。
- **バラ売り購入(buyIndividual)系共通化リファクタ**: 前回スコープ外としたショップ購入系7関数を改めて精査し、`buyIndividualItem`/`buyIndividualRite`/`buyIndividualRevelationHold`/`buyIndividualOracleHold`の4関数(kind判定・容量チェック式・価格関数・格納先フィールド名以外は完全に同型)を`buyIndividualHold`ヘルパーに委譲する純粋リファクタを実施した。`buyRelic`(別配列・別オブジェクト形状)・`buyIndividualRevelationUse`(wave即時適用の副作用)・`buyIndividualOracleUse`(oracleLevels/wave同期の副作用)は本質的に非対称なため引き続きスコープ外とした。3回にわたる一連のリファクタを通算し、`engine.ts`は1811行→1795行になった。詳細は`docs/superpowers/specs/2026-08-15-shidasu-buy-individual-hold-common-refactor-design.md`を参照。
- **コア進行系(engine.ts)共通化リファクタ**: これまで未調査だったコア進行系を改めて精査し、3箇所の機械的重複を解消した。`beginRun`を`createInitialRun()`の全フィールドをスプレッドしたうえで差分5フィールド(phase/extraTableauRows/spreadId/stageStars/currency)だけ上書きする形に書き換え、`applyPlayCard`/`applyDrawStock`/`applyStuckCheck`のplayCard/drawStock呼び出し文脈算出と妨害トリガー判定を`resolvePlayContext`/`resolveActionSabotage`の2ヘルパーに、`useRite`/`useRevelation`/`useOracle`の果断・星霜加算処理を`applyDiscretionFrostBonus`ヘルパーに、それぞれ切り出した。`drawStock`のnaiveパス・`+page.svelte`の天啓プレビューパターン・`PlayArea.svelte`のアニメーション判定重複は慎重な検討が必要なため見送った。福袋pick系から数えて4回にわたる一連のリファクタを通算し、`engine.ts`は1811行→1760行になった。詳細は`docs/superpowers/specs/2026-08-15-shidasu-core-engine-common-refactor-design.md`を参照。
- **PlayArea.svelteアニメーション中判定共通化リファクタ**: `PlayArea.svelte`にあった、5つのアニメーション状態(playingAnimation/scoreReveal/cleanupAnimation/chainResetAnimation/dealAnimationActive)から「いずれかが進行中か」を判定する論理式の重複(通常形・否定形あり、5箇所)を、単一の`$derived`変数`anyAnimationActive`に集約した。`drawStock`のnaiveパス・`+page.svelte`の天啓プレビューパターンは引き続き慎重な検討が必要なため見送った。福袋pick系から数えて5回にわたる一連のリファクタセッションはこれで一区切りとした。詳細は`docs/superpowers/specs/2026-08-15-shidasu-playarea-animation-flag-refactor-design.md`を参照。
- **playCard/drawStock安全断片共通化リファクタ**: `drawStock`のnaiveパス(パターン継続時のドロー処理)を精読したところ、`playCard`のスコア計算パイプラインを単純に再実装しているのではなく、意図的に縮小した別の計算式(適用される乗数がコンボ倍率・マンナズの2個のみ、`playCard`側は7個)になっていることが判明し、naiveパス全体の共通化は不採用とした。代わりに両者で完全一致する3つの小さな断片(橋の護符による長さ補正・神託レベル解決クロージャ・庇護/大地によるコンボ下駄履かせ)を`resolveBridgeAdjustedLengths`/`makeOracleLevelResolver`/`applyProtectionEarthFloor`の3ヘルパーに切り出した。スコア計算式の適用順序・組み合わせは一切変更していない。なお`startWave`内(`effectiveStairMinLenAtDeal`等)にも橋補正と完全一致する計算がもう1箇所残っており、次回リファクタ候補として記録しておく。詳細は`docs/superpowers/specs/2026-08-15-shidasu-scoring-helper-common-refactor-design.md`を参照。
- **startWave橋補正共通化リファクタ**: 前回記録した次回候補を回収し、`startWave`内に残っていた橋の護符による長さ補正計算(`effectiveStairMinLenAtDeal`/`effectiveSuitColorMinLenAtDeal`)を、既存の`resolveBridgeAdjustedLengths`ヘルパーの呼び出しに置き換えた(新規ヘルパー追加なし)。これにより橋補正ロジックは`playCard`・`drawStock`・`startWave`の3箇所全てで単一情報源化された。福袋pick系から数えて7回にわたる一連のリファクタセッションはこれで一区切りとした。詳細は`docs/superpowers/specs/2026-08-15-shidasu-startwave-bridge-length-refactor-design.md`を参照。
- **天啓プレビューwave一時差し替え共通化リファクタ**: 過去複数回のセッションで「リスクがありそう」として見送られてきた`+page.svelte`の天啓プレビュー用「run.waveを一時的にプレビュー用waveへ差し替えて効果適用関数を呼び、結果からwave以外を本番runへ反映してwaveだけ元に戻す」パターンを精読したところ、出現箇所は従来認識していた3箇所ではなく4箇所(`handleUseRiteInPreview`を新たに検出)で、いずれも差し替え・復元の核4行が完全一致していることが判明した。差し替え後の`previewResultWave`反映ロジック(2分岐/3分岐、呼び出し元ごとに意味が異なる)はそのまま残し、核部分のみ`applyToRevelationPreview`ヘルパーへ切り出した。福袋pick系から数えて8回にわたる一連のリファクタセッションはこれで一区切りとした。詳細は`docs/superpowers/specs/2026-08-18-shidasu-revelation-preview-wave-swap-refactor-design.md`を参照。
- **riteEffects.ts applyRiteEffect Record化リファクタ**: 以前実施済みの`sabotageEffects.ts`(switch→`Record<SabotageActionId, Handler>`化)と全く同じ形の重複が`riteEffects.ts`の`applyRiteEffect`(24ケース、各秘儀を個別`applyXxx`関数へディスパッチするswitch文)にも残っていたのを解消した。統一シグネチャ`(wave, params, rand) => WaveState`の薄いラッパー関数からなる`Record<RiteId, RiteHandler>`に置き換え、`RiteId`24種のキー漏れをコンパイル時に検出できるようにした。個々の`applyXxx`関数24個の実装は一切変更していない。同種の候補`revelationEffects.ts`の`applyRevelationEffect`(no-op重複9ケース・targetColガード重複6ケースあり)は今回のスコープ外とし、次回リファクタ候補として記録しておく。詳細は`docs/superpowers/specs/2026-08-18-shidasu-rite-effects-record-refactor-design.md`を参照。
- **revelationEffects.ts applyRevelationEffect Record化リファクタ**: 前回記録した次回候補を回収し、`revelationEffects.ts`の`applyRevelationEffect`(28ケース、`RevelationId`全種を網羅するswitch文)を`riteEffects.ts`と同じ方針で`Record<RevelationId, RevelationHandler>`に置き換えた。内訳は、Phase B天啓(即時報酬獲得系、実処理は`engine.ts`の`grantRevelationReward`側)による完全一致no-op9ケース(`kyo`・`subaru`・`ryuu`・`hotori`・`chou`・`yoku`・`mitsu`・`karasu`・`oni`)を単一の共有`noop`ハンドラに統合し、列選択必須の天啓9ケースの`targetCol === null`ガード、および列選択不要の天啓10ケースはそれぞれ元のswitch caseと同じ形のまま個別ハンドラとして移設した。個々のヘルパー関数(`convertColumnToSuit`など)の実装は一切変更していない。福袋pick系から数えて2回連続でswitch→Record化パターンのリファクタを実施し、これで一区切りとした。詳細は`docs/superpowers/specs/2026-08-18-shidasu-revelation-effects-record-refactor-design.md`を参照。
- **shop価格関数/riteEffects再配布/revelationEffectsガード共通化リファクタ**: shidasuソース全体を改めて広く調査してまとめた候補メモ(`docs/shidasu/shidasu-refactor-candidates.md`)から3件を一括で実装した。(1) `shop.ts`の`itemBuyPrice`/`itemSellPrice`が既存の`categoryPrice`ヘルパー(秘儀/天啓/神託向けに導入済み)に未移行だったのを統一。(2) `riteEffects.ts`の`applyWunjo`/`applyHagalaz`/`applyKenaz`にあった「列の長さを維持したまま1本のプールから再配布する」完全一致ループを`redistributeAcrossTableau`ヘルパーへ共通化(プールの組み立て方は各関数に残した)。(3) `revelationEffects.ts`の`REVELATION_HANDLERS`にあった9箇所の`targetCol === null`ガード重複を高階関数`withTarget`でラップし解消(内側のハンドラでは`targetCol`が`number`型に絞り込まれる)。3件とも個々の計算ロジック・ヘルパー関数の実装は一切変更していない。福袋pick系から数えて12回にわたる一連のリファクタセッションはこれで一区切りとした。詳細は`docs/superpowers/specs/2026-08-18-shidasu-price-redistribute-target-guard-refactor-design.md`を参照。
- **妨害行動の裏向き挙動(データ・表示)実装**: 星の妨害行動「総戻し」「一列戻し」「捨て札埋没」の候補段階からの「裏向きで再配布する」という説明を、実際に裏向き表示として実装した。`Card`型に`faceUp?: boolean`を追加し、3妨害行動がシャッフル後に配るカードへ設定する。表示側(`PlayArea.svelte`)は「場札の列の一番上は常に表向き、それ以外は`faceUp`に従う」という判定を都度計算する設計とし、エンジン側で`faceUp`を書き戻す処理を持たせないことで、`tiwaz`(列反転)等の他の並べ替え効果が後から発動しても自動的に正しい見た目になるようにした。`CardFace.svelte`にSolitaire由来の裏面デザイン(紺地+インディゴ格子柄)を追加。あわせて、護符の妨害行動「護符並び替え」の情報非開示表現を、テキストのみの「？？？」表示から専用の裏面デザイン(アンバー系斜めストライプ柄)に差し替え、現状HUDにしか適用されていなかった非表示判定をショップ画面の護符並べ替えUI(売却ボタンの金額表示含む)にも適用した。裏向き化・表向き化に伴うアニメーション(山札への収束演出・裏向き配布演出・フリップ演出)は別セッションで実装する。詳細は`docs/superpowers/specs/2026-08-18-shidasu-facedown-cards-items-design.md`を参照。
- **妨害行動「総戻し」「一列戻し」の裏向き演出(アニメーション)実装**: 前回実装した裏向き挙動(データ・表示)の続きとして、対象カードが山札へ収束→裏向きで場札へ配布→列の一番上だけフリップして表向きになる、という3段階演出を実装した。`WaveState`に発動検知用の`lastSabotage: {id, seq}`フィールドを追加して`triggerSabotage`が更新し、`PlayArea.svelte`側は既存の`chainResetAnimation`が使う`$effect.pre`パターン(DOM再描画前に旧カードの座標をスナップショットする)で検知した。収束フェーズは既存の`runGatherAndMoveAnimation`(片付けアニメーションが使用)を、裏向き配布フェーズは既存の`dealOneCard`/`startDealAnimation`(Wave開始時の配布アニメーションが使用、`faceUp`引数と`onLanded`フックを追加拡張)をそれぞれ再利用した。フリップ演出はSvelteの`transition:`ディレクティブを使わず、既存コードの流儀通りJS駆動の数値遷移(回転角度を0→90→0と段階的に変化させ、真横=不可視のタイミングで裏面から表面の中身に差し替える)で実装した。あわせて`/admin/shidasu-debug`に全妨害行動の直接発動ボタンを追加し、今後の妨害関連作業でも動作確認しやすくした。「捨て札埋没」は場札への配布を伴わないため今回のスコープ外とした。詳細は`docs/superpowers/specs/2026-08-18-shidasu-facedown-redistribute-animation-design.md`を参照。
  (2026-08-19追記) 初回実装では対象列の特定を`Card.faceUp`フラグ(`faceUp===false`を含む列)からの逆算に頼っていたが、過去の別トリガーで裏向きのまま残っているカードとも区別が付かず、無関係な列までアニメーション対象に巻き込む不具合があった。`SabotageResult`に`affectedTableauCols`を追加し、`applyColumnReturn`/`applyTableauFullReturn`が実際に触った列を明示的に伝える設計へ修正した。あわせて、対象列全体を隠す`sabotageAnimatingColumns`のクリアタイミングが配布フェーズの全カード着地完了まで遅延していたため、対象枚数が多い「総戻し」で「配布完了まで何も表示されない」不具合も修正した(配布フェーズ開始時点でクリアし、以後は`dealtCells`ベースの段階的表示に委ねる)。
- **大量放出・少量放出の山札→捨て札裏向き移動→フリップ演出実装**: 妨害行動「大量放出」(`stockPurge`)・「少量放出」(`stockPurgeSmall`)にも、山札から捨て札への裏向き移動演出を追加した。移動するカードの表裏は移動先(捨て札)の現在の状態を継承する設計とし(捨て札が通常時は表向き、`捨て札埋没`等で裏向き継続中ならそのまま裏向き)、`SabotageResult`に`purgedToDiscardCount`を追加して`lastSabotage`経由でUIへ伝えた(前回の`affectedTableauCols`と同じ設計方針)。移動アニメーションは`dealOneCard`と同じ手法で山札位置から捨て札位置へ個別に飛ばす専用関数、フリップ演出は前回実装した場札トップのフリップ(`startFlipReveal`)を捨て札1箇所用に単純化した専用state(`discardFlip`)で再現した。詳細は`docs/superpowers/specs/2026-08-19-shidasu-stockpurge-discard-animation-design.md`を参照。
